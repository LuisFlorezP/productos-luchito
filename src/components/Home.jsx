import Header from "./Header";

const Home = () => {
    return (
        <section>
            <Header />
            <section id="inicio"></section>
        </section>
    );
};

export default Home;